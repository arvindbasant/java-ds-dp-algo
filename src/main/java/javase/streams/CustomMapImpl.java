package javase.streams;

import java.util.function.Function;

public class CustomMapImpl implements CustomMap {

    @Override
    public <T, R> R map(Function<? super T,? extends R> function) {

        return null;
    }
}
